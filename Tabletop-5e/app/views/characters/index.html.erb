<% provide(:title, "Character List") %>
<h1>Character Index</h1>
<ul>
  <% Character.all.each do |c| %>
    <li><%= link_to(c.name, c) + ", Level " + exp_to_level(c.experience_points).to_s + ", " + c.race + ", " + c.character_class %></li>
  <% end %>
</ul>